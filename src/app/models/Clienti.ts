export interface IClienti { // Interfaccia di Articoli in TypeScript. Mettiamo la I all'inizio per distinguire degli altri
    nome: string
    cognome: string
    codCliente: string
    eta: number
    dataNascita: Date
    durataNoleggio: number
    dataNoleggio: Date
}